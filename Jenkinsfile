pipeline {
    options {
        buildDiscarder(logRotator(numToKeepStr: "5"))
    }
    agent any

    stages {       

         stage('Logging into AWS ECR') {
            steps {
		echo "ggg"
            }
        }  
}
